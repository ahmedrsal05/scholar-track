services:
  # Internal Database for Kestra metadata
  kestra_postgres:
    image: postgres:17-alpine
    volumes:
      - kestra_postgres_data:/var/lib/postgresql
    environment:
      POSTGRES_DB: kestra
      POSTGRES_USER: kestra
      POSTGRES_PASSWORD: k3str4
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # The Orchestrator
  kestra:
    image: kestra/kestra:v1.3.30
    user: "root"
    command: server standalone --flow-path /app/flows
    env_file:
      - .env_encoded
    volumes:
      - kestra_data:/app/storage
      - ./flows:/app/flows:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/kestra-wd:/tmp/kestra-wd
    environment:
      # ENV_ values are available in flows through {{ envs.* }}.
      ENV_GCP_PROJECT_ID: ${GCP_PROJECT_ID}
      ENV_GCP_BUCKET_NAME: ${GCP_BUCKET_NAME}
      ENV_GCP_DATASET: ${GCP_DATASET}
      ENV_GCP_LOCATION: ${GCP_LOCATION}
      KESTRA_CONFIGURATION: |
        datasources:
          postgres:
            url: jdbc:postgresql://kestra_postgres:5432/kestra
            driverClassName: org.postgresql.Driver
            username: kestra
            password: k3str4
        kestra:
          repository:
            type: postgres
          storage:
            type: local
            local:
              basePath: "/app/storage"
          queue:
            type: postgres
          tasks:
            tmpDir:
              path: /tmp/kestra-wd/tmp
    ports:
      - "8080:8080"
    depends_on:
      kestra_postgres:
        condition: service_healthy

volumes:
  kestra_postgres_data:
  kestra_data:
